# Voxa — Streamlit app runtime dependencies
# Pinned to exact versions (15 Aug 2026) rather than open >= ranges — a
# fresh Railway build previously had no guarantee of installing the same
# streamlit/anthropic SDK release two deploys apart. Bump deliberately,
# test locally, then commit the new pin — never let a redeploy silently
# pick up a new major/minor release.
streamlit==1.61.1
anthropic==0.122.0
httpx==0.28.1

# Test/dev dependencies for the packages/ subpackages (voxa-api, voxa-rendering,
# voxa-core, etc). Not needed to run the live Streamlit app itself, but
# required for `pytest` to collect and run the full test suite from a clean
# clone, per conftest.py's auto-bootstrap. Kept in sync with the runtime
# dependencies pyproject.toml already declares for those subpackages.
fastapi==0.141.1
pydantic==2.13.4
structlog==26.1.0
redis==8.1.0
pytest-asyncio==1.4.0

# Cross-session persistence — device cookie + Supabase, see persistence.py
supabase==2.23.0
# Restore-subscription magic-link emails — same package/version as
# clearance-app's send_report_email, reused deliberately (see
# _send_restore_email in stripe_subscription.py)
# Pinned exact (2 Sept 2026 hardening pass) — was >=6.11.0, the one
# remaining open range in this file; same class of risk that broke
# render-cap counting via an unpinned supabase-py minor bump.
sendgrid==6.12.5
streamlit-cookies-controller==0.0.4

# Subscription checkout/verification (Step 4, stripe_subscription.py) —
# same version pinned on aqe-app/clearance-app, the two other products
# in this account already using it. Kept in sync deliberately: this
# pin has already been the source of one cross-product debugging loop
# (metadata.get() vs subscript access) and drifting it here would risk
# repeating that with a fresh version's behaviour.
stripe==15.3.0

# Branded PDF export of the authenticity report (authenticity_report.py's
# export_authenticity_report_pdf, 27 Aug 2026) — reportlab is a runtime
# dependency (app.py's PDF download button calls it directly); pdfplumber
# is test-only, used by tests/unit/test_authenticity_report.py to extract
# and assert on real text from the generated PDF bytes, not just check
# they don't crash — same "test/dev deps live here too" convention as the
# packages/ block above.
reportlab==4.4.10
pdfplumber==0.11.9

# Post-deploy smoke test (dev_tools/smoke_test.py, 3 Sept 2026 hardening
# pass) — checks a live WebSocket handshake against voicova.com, the
# exact failure mode that broke app-wide on 1 Sept undetected. Test/dev
# only, not imported by the live app itself.
websocket-client==1.9.2

# Shareable Voice DNA card (voice_dna_card.py, 27 Aug 2026) — runtime
# dependency, app.py's "Share your Voice DNA" download button on the My
# Voice screen calls it directly.
Pillow==12.3.0
