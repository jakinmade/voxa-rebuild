[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "voxa-calibration"
version = "1.0.0"
description = "Voxa — voxa-calibration"
requires-python = ">=3.12"
dependencies = ["voxa-core", "pydantic>=2.0", "structlog>=24.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/voxa_calibration"]
