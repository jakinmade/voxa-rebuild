[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "voxa-humanisation"
version = "1.0.0"
description = "Voxa Layer 1 — Humanisation Engine"
requires-python = ">=3.12"
dependencies = [
    "voxa-core",
    "pydantic>=2.0",
    "structlog>=24.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/voxa_humanisation"]
