[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "voxa-rendering"
version = "1.0.0"
description = "Voxa Layer 3 — Voice Rendering Engine"
requires-python = ">=3.12"
dependencies = [
    "voxa-core",
    "pydantic>=2.0",
    "structlog>=24.0",
    "httpx>=0.27.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/voxa_rendering"]
